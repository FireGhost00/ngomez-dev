---
interface Props {
	date: Date;
}

const { date } = Astro.props;
// El prototipo muestra la fecha en formato ISO corto: 2026-07-02
const iso = date.toISOString().slice(0, 10);
---

<time datetime={iso}>{iso}</time>
